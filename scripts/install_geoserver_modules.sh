docker cp *.jar geoserver:/usr/local/tomcat/webapps/geoserver/WEB-INF/lib/

TODO:
- Download all of the modules and extensions to the locahost
- Unzip and prepare for transfer to the docker host, then the docker container running geoserver
- Load all of the datasets that John Ritson sent through to me yesterday
- Make menu switches for the new datasets in the WebUI